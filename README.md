# berkeley-restaurant-visualizer

The core logic lives in project.py files, which handle data abstractions and machine learning algorithms for predicting restaurant scores. The remaining files handle utility functions, visualization, and the Yelp dataset.
