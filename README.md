A visualization of restaurant scores across Berkeley using machine learning and the Yelp academic dataset, constructed as a Voronoi diagram.

The core logic lives in the project.py files, which handle data abstractions and ML algorithms for predicting restaurant scores. The remaining files handle utility functions, visualization, and the Yelp dataset.
